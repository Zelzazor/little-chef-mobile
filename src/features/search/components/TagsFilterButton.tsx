import { Button } from '@rneui/base';
import Icon from 'react-native-vector-icons/Ionicons';

export const TagsFilterButton = () => {
  return (
    <Button>
      <Icon name="filter-outline" />
    </Button>
  );
};
