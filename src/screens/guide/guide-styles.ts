import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  titleText: {
    fontSize: 28,
    fontWeight: 'bold',
    marginBottom: 8,
  },
  text: {
    fontSize: 20,
    marginBottom: 8,
  },
  lastParagraph: {
    fontSize: 20,
    marginBottom: 32,
  },
  image: {
    width: '100%',
    borderRadius: 10,
    height: 850,
    marginBottom: 24,
  },
});

export default styles;
